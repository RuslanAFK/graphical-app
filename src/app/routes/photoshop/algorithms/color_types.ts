export type Hsv = {
  h: number,
  s: number,
  v: number
}

export type Cmyk = {
  c: number,
  m: number,
  y: number,
  k: number
}

export type Hsl = {
  h: number,
  s: number,
  l: number
}

export type Rgb = {
  r: number,
  g: number,
  b: number,
  a?: number
}
